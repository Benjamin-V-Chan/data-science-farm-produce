
# 1. Import pandas and numpy
# 2. Read raw CSV from '../data/farm_production_dataset.csv'
# 3. Inspect and report missing values
# 4. Drop rows or impute missing numeric values (e.g. with median)
# 5. Convert year column ('REF_DATE') to datetime
# 6. Standardize column names (lowercase, underscores)
# 7. Create derived features:
#      - production_per_ha = total_production / seeded_area_ha
#      - value_per_tonne  = total_farm_value / total_production
# 8. Save cleaned DataFrame to '../outputs/processed_data.csv'
